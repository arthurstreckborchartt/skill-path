# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Skills de agente instaladas localmente (npx skills add) — ferramenta da máquina, não do produto
.agents/
.claude/skills/
skills-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Segredos locais.
# ATENÇÃO: o .env deste repositório ESTÁ versionado e vai para o GitHub. Ele só pode conter
# valores públicos (URL do Supabase, chave publishable, DSN do Sentry). Chave secreta —
# ANTHROPIC_API_KEY, service role do Supabase, client secret do Google — vai em .env.local
# no desenvolvimento e no painel de segredos do provedor em produção.
.env.local
.env.*.local

# O token da ponte local. Ele autentica uma maquina inteira contra o Pathly, e o servidor guarda
# so o hash dele — commitar este arquivo entregaria o segredo que nem o Pathly tem.
ponte/.pathly-bridge.json
